# Точка отката — Zindan 1.3.10 (versionCode 140)

**Дата фиксации:** 2026-06-09  
**Устройства полевого теста:** Samsung Galaxy S24 Ultra, Tab S9 FE+  
**Git-тег:** `v1.3.10-baseline`

Используйте этот коммит/тег, если эксперименты с Anti Spy / VPN ухудшат поведение и нужно вернуться к состоянию **после многодневного реального использования**.

---

## Сборка

```powershell
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"
# Optional: $env:GRADLE_USER_HOME = ".gradle-user-home"
.\gradlew.bat :app:assembleDebug
```

APK: `app\build\outputs\apk\debug\app-debug.apk` (копия: `Zindan-1.3.10-debug.apk`).

---

## Подтверждено работает (полевой тест)

| Область | Статус |
|---------|--------|
| Рабочий профиль, клонирование, заморозка/разморозка | ✅ |
| Список автозаморозки, групповая заморозка (снежинка) | ✅ |
| Автозаморозка при блокировке экрана (после «Разморозить и запустить») | ✅ |
| Anti Spy: запуск приложения **при уже включённом VPN** | ✅ (блок/снятие VPN перед запуском) |
| Диалоги Anti Spy на экране (1.3.10), не в шторке | ✅ (когда цепочка доходит до UI) |
| Настройки «О программе»: только версия и исходный код | ✅ |
| USER_GUIDE.md / PDF | ✅ |

---

## Известный пробел — сценарий 2 (не исправлять «случайно» при откате)

**Условие:** приложение из списка автозаморозки **уже работает или в фоне** → пользователь **включает VPN** (золотой щит включён).

**Ожидание пользователя:** автоматически «Заморозить всё».

**Факт в 1.3.10:**

- Автоматический вызов `freezeAllForVpn()` **не подключён** (метод есть, не вызывается из VPN-монитора).
- Заложен полуручной путь: dummy-VPN → диалог «Да/Нет» → заморозка только по «Да».
- Диалог «Да/Нет» **практически не появляется**, если:
  - dummy-VPN не снял сторонний туннель или клиент сразу переподключился;
  - ранее нажато «Нет» (повторы глушатся до выключения VPN);
  - система блокирует `Activity` с фона (Samsung без «поверх других приложений»);
  - не живёт FGS «Контроль VPN» (батарея / ограничения фона).

**Детекция VPN** (как MacroDroid) в коде **есть**; проблема в **реакции** на событие, не в отсутствии монитора.

Подробнее: [PROBLEMS.md](PROBLEMS.md) → раздел «Baseline 1.3.10».

---

## Откат в git

```powershell
git checkout v1.3.10-baseline
```

`git reset --hard` уничтожит незакоммиченные изменения — используйте осознанно.

---

## Следующий шаг разработки (после этой точки)

Доработка **только сценария 2**: VPN-up при работающем/фоновом приложении — показ диалога без жёсткого условия «VPN уже снят» и/или опциональная фоновая `freezeAllForVpn()`.

Сценарий «VPN уже есть → запуск приложения» **не трогать** без явной необходимости.

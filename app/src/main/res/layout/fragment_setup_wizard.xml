<?xml version="1.0" encoding="utf-8"?>
<!--
    Мастер первой настройки. Раньше экраны строились на вендоренной
    com.android.setupwizardlib (модуль :setup-wizard-lib): она давала header,
    полосу прогресса и нижнюю панель Back/Next. Здесь то же самое собрано из
    Material 3, поэтому модуль больше не нужен.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/wizard_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/setupWizardContentBackground">

    <LinearLayout
        android:id="@+id/wizard_step_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="12dp">

        <com.google.android.material.progressindicator.LinearProgressIndicator
            android:id="@+id/wizard_progress"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            app:trackCornerRadius="2dp"
            app:trackStopIndicatorSize="0dp"
            app:trackThickness="4dp"
            app:indicatorColor="@color/setupWizardHeaderProgress"
            app:trackColor="@color/md_outlineVariant" />

        <TextView
            android:id="@+id/wizard_step_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:textAppearance="?attr/textAppearanceLabelMedium"
            android:textColor="@color/md_onSurfaceVariant" />

    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="24dp"
        android:clipToPadding="false"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/wizard_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textAppearance="?attr/textAppearanceHeadlineSmall"
                android:textColor="@color/setupWizardContentText" />

            <TextView
                android:id="@+id/wizard_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:lineSpacingMultiplier="1.15"
                android:textAppearance="?attr/textAppearanceBodyMedium"
                android:textColor="@color/md_onSurfaceVariant" />

            <LinearLayout
                android:id="@+id/wizard_checks"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:orientation="vertical"
                android:visibility="gone" />

        </LinearLayout>

    </ScrollView>

    <LinearLayout
        android:id="@+id/wizard_actions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="16dp"
        android:paddingBottom="24dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/wizard_next"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            app:cornerRadius="24dp"
            android:textAppearance="?attr/textAppearanceLabelLarge" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/wizard_back"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginTop="8dp"
            app:cornerRadius="24dp"
            android:textAppearance="?attr/textAppearanceLabelLarge"
            android:text="@string/wizard_back" />

    </LinearLayout>

</LinearLayout>

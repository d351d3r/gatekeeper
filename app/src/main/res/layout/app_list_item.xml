<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:layout_width="match_parent"
    android:layout_height="64dp">

    <ImageView
        android:id="@+id/list_app_icon"
        android:scaleType="fitCenter"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <TextView
        android:id="@+id/list_app_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="8dp"
        android:textAppearance="?attr/textAppearanceBodyLarge"
        android:textColor="?attr/colorOnSurface"
        android:maxLines="1"
        android:ellipsize="end"
        app:layout_constraintStart_toEndOf="@id/list_app_icon"
        app:layout_constraintEnd_toStartOf="@id/list_app_freeze"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/list_app_perms_row"
        app:layout_constraintVertical_chainStyle="packed" />

    <!-- Строка глифов под именем: значки объявленных приложением опасных разрешений.
         Наполняется в адаптере. -->
    <LinearLayout
        android:id="@+id/list_app_perms_row"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="8dp"
        android:layout_marginTop="2dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:visibility="gone"
        app:layout_constraintStart_toEndOf="@id/list_app_icon"
        app:layout_constraintEnd_toStartOf="@id/list_app_freeze"
        app:layout_constraintTop_toBottomOf="@id/list_app_title"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Кнопка заморозки/разморозки: снежинка = заморозить работающее, оттаивание =
         разморозить замороженное. Тап переключает. -->
    <ImageView
        android:id="@+id/list_app_freeze"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginEnd="12dp"
        android:padding="8dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_freeze"
        app:tint="?attr/colorOnSurfaceVariant"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/freeze_app"
        android:visibility="gone"
        app:layout_constraintEnd_toStartOf="@id/list_app_select_order"
        app:layout_goneMarginEnd="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <TextView
        android:id="@+id/list_app_select_order"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginEnd="16dp"
        android:textColor="?attr/colorOnPrimary"
        android:textStyle="bold"
        android:background="@drawable/bg_select_order"
        android:gravity="center"
        android:textSize="12sp"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>

<?xml version="1.0" encoding="utf-8"?>
<!--
    2x2 по умолчанию, тянется до 4x1: и счетчик, и строка состояния из одного
    макета. Обновление по запросу приложения, периодического нет: счетчики
    меняет только действие пользователя, будить виджет по таймеру незачем.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="40dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="0"
    android:previewLayout="@layout/widget_freeze"
    android:description="@string/widget_description"
    android:initialLayout="@layout/widget_freeze"
    android:widgetCategory="home_screen" />
